{
  "name": "viavds",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon --watch src --ext js,json --exec \"node src/app.js\"",
    "test": "echo \"no tests\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
